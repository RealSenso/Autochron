# Environment variables used by the application
# Copy this file to .env and fill in the values

# Disable Hot Module Replacement (automatically set in the AI Studio sandbox)
DISABLE_HMR=true

# Firebase API Key (if needed for custom client override, otherwise uses firebase-applet-config.json)
FIREBASE_API_KEY=
